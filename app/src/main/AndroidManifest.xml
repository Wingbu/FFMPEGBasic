<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wingbu.ffmpegbasic">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".file.FileIndexActivity" />
        <activity android:name=".play.VideoPlayActivity" />
        <activity android:name=".play.VideoViewActivity" />
        <activity android:name=".trans.TransVideoActivity" />
        <activity android:name=".play.MixPlayActivity" />
        <activity android:name=".opensl.OpenSLActivity" />
        <activity android:name=".play.RawPlayerActivity"></activity>
        <activity android:name=".IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".parse.H264ParseActivity"></activity>
    </application>

</manifest>